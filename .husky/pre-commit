# `npm run gate` is lint (prettier --check + eslint), svelte-check and tests —
# the same things CI runs. It reports bad formatting rather than rewriting
# files mid-commit; run `npm run format` to fix them.
npm run gate
